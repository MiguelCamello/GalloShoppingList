git config --global user.name MiguelLima
git config --global user.email mlimacamello@gmail.com


criação do projeto:
    npx create-expo-app@latest GalloShoppingList --template blank


instalar dependencia:
    npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar


Testar projeto no emulador:
    npx expo start --tunnel --android


Toda vez que abrir o projeto:
    cd GalloShoppingList
    npm i